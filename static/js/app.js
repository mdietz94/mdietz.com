// Generated by CoffeeScript 1.6.2
var currentPage, updatePages;

currentPage = 0;

updatePages = function() {
  $("#content-main").css('left', "" + (currentPage * -200) + "%");
  $("#content-left").css('left', "" + ((1 + currentPage) * -200) + "%");
  return $("#content-right").css('left', "" + ((-1 + currentPage) * -200) + "%");
};

$(function() {
  updatePages();
  $("#page-left").mousedown(function() {
    if (currentPage > -1) {
      currentPage -= 1;
      return updatePages();
    }
  });
  return $("#page-right").mousedown(function() {
    if (currentPage < 1) {
      currentPage += 1;
      return updatePages();
    }
  });
});
